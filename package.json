{
  "name": "octo-santa",
  "description": "Local-first agent collaboration framework — messaging, channels, and an interactive REPL, all backed by SQLite",
  "version": "0.6.0",
  "module": "src/mcp.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/mcp.ts",
    "build": "bun run scripts/build.ts",
    "build:mcp": "bun run scripts/build.ts mcp",
    "build:repl": "bun run scripts/build.ts repl",
    "start:repl": "bun run src/repl/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  }
}
