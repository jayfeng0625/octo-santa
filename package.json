{
  "name": "octo-santa",
  "description": "Local-first agent messaging over MCP — channels, DMs, and push notifications, all backed by SQLite",
  "version": "0.8.0",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "build": "bun run scripts/build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "archunit": "^2.1.66"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  }
}
